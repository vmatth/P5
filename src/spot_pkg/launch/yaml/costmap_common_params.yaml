obstacle_range: 10 #2.5
raytrace_range: 10.5 #3

#footprint: [[-0.555, -0.25], [-0.555, 0.25], [0.555, 0.25], [0.555, -0.25]]

footprint: [[-0.4, -0.2], [-0.4, 0.2], [0.4, 0.2], [0.4, -0.2]]
#robot_radius: 0.5
# inflation_radius: 0.6

# observation_sensor: laser_scan_sensor

# laser_scan_sensor: {sensor_frame: sick_tim, data_type: LaserScan, topic: /scan, marking: true, clearing: true}

inflation:
  inflation_radius: 0.05 #0.1
  cost_scaling_factor: 20  # exponential rate at which the obstacle cost drops off (default 10)

obstacle_2d_layer:
    observation_sources: scan
    scan: {data_type: LaserScan, sensor_frame: sick_tim, topic: /scan, marking: true, clearing: true, min_obstacle_height: 0.0, max_obstacle_height: 0.5}
s
plugins: 
    - {name: static_map, type: "costmap_2d::StaticLayer"}
    - {name: obstacle_2d_layer, type: "costmap_2d::ObstacleLayer"}
    - {name: inflation, type: "costmap_2d::InflationLayer"}
    #- {name: simple_layer, type: "simple_layer_namespace::SimpleLayer"}

